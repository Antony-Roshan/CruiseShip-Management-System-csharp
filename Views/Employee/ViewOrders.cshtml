@model IEnumerable<CruiseshipApp.Models.OrderStackBookingDetails>

@{
        ViewBag.Title = "ViewOrders";
        Layout = "~/Views/Items/Items_LayoutPage.cshtml";
    }

<center>
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />

    <h1>View Orders</h1>
    <br />

    @if (TempData["AlertMessage"] != null)
    {
        <div class="alert alert-info ">
            <strong>@TempData["AlertMessage"]</strong>
        </div>
    }

    <table class="table">
        <tr>
            @*<th>
                    @Html.DisplayNameFor(model => model.Order_id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Voyager_id)
                </th>*@
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Total)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                @*            <td>
            @Html.DisplayFor(modelItem => item.Order_id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Voyager_id)
        </td>*@
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Date)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Total)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Status)
                </td>

                @*<td>
                    @Html.ActionLink("Order Details", "ItemOrderDetails", new { id = item.Order_id })
                </td>
*@
                <td>
                    <a href="ItemOrderDetails?id=@item.Order_id" class="btn btn-success">Order Details</a>
                </td>
                @if (item.Status == "Delivered")
                {

                }
                else
                {
                    @*<td>
                        @Html.ActionLink("Deliver", "Deliver", new { id = item.Order_id })
                    </td>*@
                    <td>
                        <a href="Deliver?id=@item.Order_id" class="btn btn-success">Deliver</a>
                    </td>
                }

            </tr>
        }

    </table>

</center>